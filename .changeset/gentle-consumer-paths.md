---
"@lucas-barake/effect-local": minor
"@lucas-barake/effect-local-sql": patch
"@lucas-barake/effect-local-test": minor
---

Allow replica definitions to omit empty mutation, projection, and query collections.

Reject duplicate SQL bindings when one binding is required for each projection.

Install declared SQL projection binding Layers automatically in every TestReplica constructor.
