---
"@lucas-barake/effect-local": minor
"@lucas-barake/effect-local-browser": minor
---

Use Effect Schema defect causes directly in replica errors and remove the redundant structured cause wrappers.

Bump the browser RPC protocol to version 3 and report rolling version mismatches as typed `ProtocolMismatch` errors.
